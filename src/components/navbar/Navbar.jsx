function Navbar() {
  return (
    <header>
      <nav>
        <a href="/#"></a>
        <a href="/#">Home</a>
        <a href="/#">Products</a>
        <a href="/#">Sale</a>
        <a href="/#">About</a>
        <button>Login</button>
        <button>Login</button>
      </nav>
      </nav>
    </header>
  );
}

export default Navbar;
