import React from 'react';

function Navbar() {
  return (
    <header>
      <nav>
        <a href="/#">Home</a>
        <a href="/#">Products</a>
        <a href="/#">Sale</a>
        <a href="/#">About</a>
        <p>Login</p>
      </nav>
    </header>
  );
}

export default Navbar;
